<% if params[:plan] == '2' %>
  <h2>Premium Account</h2>
  <p>Sign up for the premium account for $10/month with basic access plus contact information!</p>
<% else %>
  <h2>Basic Account</h2>
<% end %>
<h2>Sign up</h2>
<% if params[:plan] == '2' %>
  <%= render "pro_form" %>
<% else %>
  <%= render "basic_form" %>
<% end %>

<%= render "devise/shared/links" %>

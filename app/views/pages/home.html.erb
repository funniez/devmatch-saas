<div class="jumbotron text-center">
<h1>Welcome to DevedIn</h1>
<h3>A membership site where entrepreneurs can meet developers.</h3>
</div>
  <div class="row">
    <% if user_signed_in? %>
      <div class="well">
        <h2 class="text-center">Your Profile</h2>
          <%= link_to "Edit your profile", "#", class: 'btn btn-default' %>
            <%= link_to "View your profile", user_path(current_user), class: 'btn btn-default' %>
    <% else %>
    <p>Create your profile so that you can share your information with the community.</p>
            <%= link_to "Create your profile", new_user_profile_path(current_user), class: 'btn btn-default'%>
          <% end %>
        </div>
      </div>
      <div class="well">
          <h2 class="text-center">The Community</h2>
          <p>Visit the community to meet other developers, entrepreneurs, and investors!</p>
          <%= link_to "Visit the community", "#", class: 'btn btn-default btn-lg btn-block' %>
      </div>
    <% else %>
    <div class="col-md-6">
      <div class="well">
        <h2 class="text-center">Basic Membership</h2>
        <h5 class="text-center">Sign up for free and get basic access to the community.</h5>
        <br />
        <%= link_to "Sign up with Basic", new_user_registration_path(plan: @basic_plan.id), class: 'btn btn-primary btn-lg btn-block' %>
      </div>
    </div>
    <div class="col-md-6">
      <div class="well">
        <h2 class="text-center">Premium Membership</h2>
        <h5 class="text-center">Sign up for the premium account for $10/month including contact information!</h5>
        <br />
        <%= link_to "Sign up with Pro", new_user_registration_path(plan: @pro_plan.id), class: 'btn btn-success btn-lg btn-block' %>
      </div>
    </div>
  <% end %>
</div>
